include <stdio.h>

struct person:
    char[20] name
    int age

int wat():
    struct person bob
    bob.name = "Bob"
    bob.age = 19
    return (char) bob.age  // Why would you ever do this??? Anyway, typecasting works normally
